# pytest 設定檔
[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    integration: 需要真實 API 金鑰才能執行的整合測試（使用 -m "not integration" 排除）
    slow: 執行較慢的測試
